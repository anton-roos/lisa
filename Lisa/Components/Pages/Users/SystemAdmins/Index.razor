@page "/system-administrators"
@using Lisa.Models.Entities
@inject NavigationManager Navigation
@inject SchoolService SchoolService
@inject UserService UserService

<h3>System Administrators</h3>
<button class="btn btn-primary" @onclick="AddSystemAdministrator">
    Add System Administrator
</button>

@if (SystemAdministrators == null)
{
    <p>Loading...</p>
}
else if (SystemAdministrators.Count == 0)
{
    <p>No System Administrators found.</p>
}
else
{
    <table class="table table-striped">
        <thead>
            <tr>
                <th>Name</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var systemAdministrator in SystemAdministrators)
            {
                <tr>
                    <td>@($"{systemAdministrator.FirstName} {systemAdministrator.LastName}")</td>
                    <ActionButtons TEntity="SystemAdministrator" Entity="systemAdministrator" OnView="ViewSystemAdministrator"
                        OnEdit="EditSystemAdministrator" OnDelete="DeleteSystemAdministrator" />
                </tr>
            }
        </tbody>
    </table>
}

@code {
    private List<SystemAdministrator>? SystemAdministrators;

    protected override async Task OnInitializedAsync()
    {
        SystemAdministrators = await UserService.GetAllByRoleAndSchoolAsync<SystemAdministrator>();
    }

    private void AddSystemAdministrator()
    {
        Navigation.NavigateTo("/system-administrators/add");
    }

    private void EditSystemAdministrator(SystemAdministrator systemAdministrator)
    {
        Navigation.NavigateTo($"/system-administrators/edit/{systemAdministrator.Id}");
    }

    private void DeleteSystemAdministrator(SystemAdministrator systemAdministrator)
    {
        Navigation.NavigateTo($"/system-administrators/edit/{systemAdministrator.Id}");
    }

    private void ViewSystemAdministrator(SystemAdministrator systemAdministrator)
    {
        Navigation.NavigateTo($"/system-administrators/view/{systemAdministrator.Id}");
    }
}
